# Todolist

## 本轮 v5.2.69（通知栏常驻+闹钟+移动到修复）

### 通知栏常驻+空闲态
- [x] 新建 `js/core/idle-bar.js`（鸡汤语录按早/午/晚分类 30 条 + 6 种宠物，点击切换）
- [x] 修改 `countdown.js` 无倒数日时显示空闲态而非隐藏
- [x] CSS 空闲态淡蓝紫渐变样式 + 闹钟橙黄渐变样式
- [x] **修复**：移除 15 秒自动轮换，改为点击才切换
- [x] **修复**：`showIdleNotice()` 每秒被 tick 调用导致内容疯狂闪烁——加 `wasIdle` 守卫只首次随机

### 自定义闹钟
- [x] 新建 `js/core/alarm.js`（每天/工作日/每周几 + 时间，提前 3 分钟闪烁）
- [x] sync.js 闹钟数据纳入 sideData 同步+备份（6 处恢复路径全覆盖）
- [x] app.js tick 增加 `checkAlarms()` 调用
- [x] 交互：双击/长按空闲态通知栏打开闹钟设置

### 移动到 Bug 修复（v5.1.68）
- [x] Bug1：日期选择器定位从屏幕中央改为跟在菜单旁边
- [x] Bug2：待办事项（含无 deadline 的）移动时实际不更新日期

### 语法检查
- [x] idle-bar.js + alarm.js + countdown.js + app.js + sync.js → 全部 OK

### 线上验证
- [x] 版本号 v5.2.69 ✅
- [x] 通知栏常驻 ✅
- [x] 鸡汤内容切换 ✅
- [x] 宠物内容切换 ✅
- [x] 右键菜单回归 ✅
- [x] 移动到子菜单回归 ✅
- [x] 无 JS 错误 ✅

### 提交记录
- `bea3276` fix: 移动到功能两个bug修复——日期选择器定位+待办类型支持（v5.1.68）
- `8763975` feat: 通知栏常驻+空闲态鸡汤宠物+自定义闹钟提醒（v5.2.69）
- `0765aa5` fix: 移除空闲态15秒自动轮换，改为仅点击切换，选定后固定不再闪烁
- `36f58ab` fix: 空闲态内容每秒闪烁——首次进入空闲态固定随机内容，后续tick不再重新随机

---

## 产品优化建议（待排期）
- 🔴 月视图日期点标记：彩色小圆点 + 点击弹出当天列表
- 🟡 跨日会议可视化：周/月视图横条展示跨天会议
- 🟡 会议提醒提前通知
- 🟡 loadItems 防抖合并
- 🟡 console.log 生产环境静默
- 🟢 周视图拖拽改会议时长
- 🟢 颜色标签自定义
- 🟢 定时器合并
- 🔵 周视图显示周数

## 长期待执行
- 🔴 PDF 部门名称误判为地点
- 🔴 Supabase CDN 本地化
- 🔴 参会人关联准确性
