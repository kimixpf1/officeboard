# Todolist

## 当前轮次目标
- 继续推进 P2 更深一层的低风险优化，在不影响现有线上功能前提下完善日历视图交互
- 修复周视图 / 月视图中新建事项后不能实时显示的问题
- 在现有左键空白快速新增基础上，增加右键点击日期格也可快速新增

## 当前待办
- 暂无

## 已完成
- ✅ 已重新通读 `.trae/rules/` 五个规则文件并继续按“验证通过后默认提交、推送、部署”执行
- ✅ 已定位日历新增后不实时显示的根因：`loadItems()` 在周 / 月视图下未同步触发 `window.calendarView.render()`
- ✅ 已修复周视图 / 月视图新增事项后实时刷新问题：保存后在日历视图下会立即重绘当前日历页
- ✅ 已为 `calendar.js` 抽出 `bindQuickAddEvents`，统一左键点击与右键点击日期格快速新增逻辑
- ✅ 已实现周视图日期格右键快速新增，且不影响已有事项点击跳转日视图
- ✅ 已实现月视图当月日期格右键快速新增，且保留 `.other-month` 补位格不承载新增
- ✅ 已完成 `node --check js/app.js`、`node --check js/calendar.js` 与 app.js / calendar.js 诊断 0 错误
- ✅ 已完成页面级真人模拟测试：
  - 周视图左键点击日期格可弹出快速新增并保存后实时显示
  - 月视图可实时显示刚新增的事项
  - 月视图右键点击日期格可弹出快速新增
- ✅ 已更新 `index.html` 资源版本号：`calendar.js v16`、`app.js v79`

## 已跳过
- 暂无

## 下一步
- 提交、推送、部署本轮日历实时刷新 + 右键快速新增优化
- 用户线上强制刷新后，重点复测：
  - 周视图新增后是否无需切换视图即可立即显示
  - 月视图新增后是否无需切换视图即可立即显示
  - 周 / 月视图右键点击日期格是否都能正常快速新增
