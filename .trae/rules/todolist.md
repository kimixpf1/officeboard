# Todolist

## 当前轮次目标
- 修复倒数日新增表单在手机端和电脑端的输入框重叠问题，确保名称框与日期框彻底分离排列
- 修复倒数日跨设备同步在“本地事项为空但已有倒数日数据”场景下被错误覆盖的问题
- 修复跨日期会议/办文在周视图、月视图中的完成态映射与作用范围更新异常
- 完成本地静态校验、模拟测试、提交推送与线上强刷验证

## 当前待办
- 待真实双端登录环境下继续验证倒数日跨设备同步新增 / 编辑 / 排序链路
- 待真实业务数据下继续验证农历编辑回填与跨日期完成态边界场景
- 待继续观察 GitHub Pages 是否仍出现旧 CSS / JS 混用缓存

## 已完成（本轮 P3-14）
- ✅ 已将倒数日新增表单改为移动端默认单列堆叠、桌面端分层双列，名称框与日期框各自独占整行
- ✅ 已为倒数日表单控件补齐 `width: 100%`、`min-width: 0`、`box-sizing: border-box` 与明确 grid 区域，避免日期控件覆盖名称输入框
- ✅ 已修复同步链路中“本地事项为空时忽略本地倒数日数据”的分支，已有倒数日数据时改为合并而非直接下载覆盖
- ✅ 已为云端倒数日数据下载补齐差异比较，仅在值变化时回写 `countdownEvents`、`countdownTypeColors`、`countdownSortOrder`
- ✅ 已修复跨日期会议完成状态更新误用办文 scoped update 的问题
- ✅ 已修复跨日期办文完成切换未正确写入作用范围状态的问题
- ✅ 已补强 `calendar.js` 对跨日期会议/办文按日投影态的映射，周/月视图可读取 `dayStates` 中的完成态与相关字段
- ✅ 已修复农历倒数日编辑时 `originalDate` 被渲染结果污染的问题
- ✅ 已将部署版本提升为 `2026-04-22 P3-14`，资源版本提升为 `calendar.js v25 / sync.js v23 / app.js v95 / style.css v31`
- ✅ 已完成 `node --check js/app.js`、`node --check js/sync.js`、`node --check js/calendar.js` 与 `index.html` / `app.js` / `sync.js` / `calendar.js` / `style.css` diagnostics 0 错误
- ✅ 已完成本地模拟测试：手机 `390x844` 与桌面 `1366x900` 下倒数日表单控件无重叠，部署徽标显示为 `P3-14`
- ✅ 已提交并推送 `07fdddfe07f77c3154d426ec12e5543c3ff99f6b fix countdown layout sync and cross-date states` 到 `main`
- ✅ 已完成线上强刷验证：线上版本徽标为 `2026-04-22 P3-14`，并成功加载 `style.css?v=31`、`calendar.js?v=25`、`sync.js?v=23`、`app.js?v=95`
- ✅ 已完成线上布局回归：手机 `390x844` 与桌面 `1366x900` 下倒数日名称框、日期框、类型框、颜色框、提交按钮均无重叠

## 已完成（本轮 P3-9 / P3-8 / 更早）
- ✅ 已完成倒数日同步、农历生日/纪念日、编辑/删除/排序/类型色等前序能力建设
- ✅ 已完成顶部布局恢复、识别预览编辑、PDF 结构化提取、周月视图完成态展示等前序修复

## 已跳过
- 暂不在本轮继续拆分 OCR、同步、表单弹窗等高耦合链路
- 暂不在本轮引入数据库 schema 变更或同步协议变更

## 下一步
- 在真实双端登录环境下继续验证倒数日跨设备同步新增 / 编辑 / 排序
- 在真实业务数据下继续验证农历编辑回填与跨日期完成态边界场景
- 继续观察 GitHub Pages 缓存表现
