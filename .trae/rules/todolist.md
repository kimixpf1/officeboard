# Todolist

## 当前轮次目标
- 完成 OCR 识别预览逐条编辑与删除能力，并打通确认后的正式入库链路
- 增强 PDF 结构化提取，提升会议安排表类 PDF 对表格列、跨行参会人员和合并单元格的恢复能力
- 完成本轮静态校验、版本提升、提交推送与线上强刷回归准备

## 当前待办
- 已完成本轮代码改造、静态校验与版本提升，待提交、推送后做线上强刷回归复测

## 已完成
- ✅ 已继续沿用可回退策略，在 `app.js` 第一层拆分基础上推进第二层低风险收口
- ✅ 已完成本地浏览器真人测试，确认此前“横线样式未生效”和“跨栏拖拽仍消失”并非已修复，而是存在真实根因
- ✅ 已修复周 / 月视图已完成事项横线样式桌面端未生效的问题：将 `.calendar-item.completed` / `.calendar-item.dragging` / 日期格 `drag-over` 全局样式补到桌面样式区，不再只存在于移动端块内
- ✅ 已修复日视图跨栏拖拽事项后消失的问题：跨类型拖拽时除更新 `type` 外，同步迁移 `deadline / date / docDate / docStartDate / docEndDate / time / progress` 等关键字段，避免事项因缺少目标类型日期字段被过滤掉
- ✅ 已修复日视图跨栏拖拽时事项直接消失的问题：拖拽开始/结束统一使用 `currentTarget`，避免事件命中子节点导致拖拽源元素记录错误
- ✅ 已修复日视图切到周视图 / 月视图时未正确定位到包含当前选中日期的问题：切换视图时统一先把 `calendarView` 对齐到 `selectedDate`
- ✅ 已在周视图 / 月视图中为已完成事项增加区分展示：已完成项自动沉底并显示横线样式
- ✅ 已为周视图 / 月视图日历事项增加拖拽能力，可直接把待办 / 会议 / 办文拖到目标日期单元格
- ✅ 已为周视图日格内事项增加拖拽排序能力，可在同一天内直接调整显示顺序并落库
- ✅ 已修复周视图 / 月视图办文完成态不生效的问题：日历映射按日期展开时补齐 dayStates 解析，办文可按当日 progress 正确打勾并沉底
- ✅ 已新增 `moveItemToDateFromCalendar(targetDate)`，统一承接从周 / 月视图拖拽改日期
- ✅ 已新增部署版本可视化徽标 `#deployVersionBadge`，页面加载后会显示当前部署版本
- ✅ 已将版本信息同步到徽标提示中，便于快速核对脚本资源版本是否已更新
- ✅ 已新增会议自动完成规则：
  - 有具体时间的会议，开始后 30 分钟自动完成
  - 无具体时间的单日会议，在当天 16:00 自动完成
  - 无具体时间的跨日期会议，按当天独立状态在当天 16:00 自动完成（今明两天会分别在各自当天 16:00 完成）
- ✅ 已修复跨日期会议手动标记完成的作用范围处理：支持仅当天 / 今天及之后 / 全部日期
- ✅ 已修复周视图 / 月视图点击具体事项跳转到错误日视图的问题：日历项点击优先使用当前格日期 `_viewDate`
- ✅ 已修复日历内部工作日判断仍走旧硬编码的问题：统一改为复用 `HolidayData`，避免与主业务节假日判断不一致
- ✅ 已修复办文卡片完成按钮状态与展示状态不一致的问题：卡片 `completed` 类、`dataset.completed` 与按钮样式统一改为按有效完成态计算
- ✅ 已按用户提供的真实 PDF `C:\Users\42151\Desktop\【4.20】近期主要会议活动安排表.pdf` 完成样本验证
- ✅ 已修复真实 PDF 识别链路运行时错误 `existTitle is not defined`
- ✅ 已优化 PDF 文本提取：按坐标排序并按行聚合，减少会议安排表表格结构被打散导致的漏识别
- ✅ 已在 `upload-flow.js` 中为识别前预览增加逐条编辑与删除能力，支持对待新增事项编辑标题、日期、时间、地点、参会人员，并可删除新增 / 合并 / 跳过项
- ✅ 已在 `app.js` 与 `wechat-upload.js` 中接入新的预览返回结构，确认后按编辑后的 `actionPlan` 正式入库，取消时仍会恢复快照
- ✅ 已在 `ocr.js` 中新增 PDF 结构化提取链路：按文本块坐标归一、按行分组、按列间距切分单元格，并补充分组 / 参会人员跨行继承与标题、地点清洗
- ✅ 已将版本信息提升为：`ocr.js v34`、`upload-flow.js v5`、`wechat-upload.js v9`、`app.js v87`、部署版本 `P3-6`
- ✅ 已完成 `node --check js/app.js`
- ✅ 已完成 `node --check js/wechat-upload.js`
- ✅ 已完成 `node --check js/upload-flow.js`
- ✅ 已完成 `node --check js/ocr.js`
- ✅ 已完成 `app.js` / `wechat-upload.js` / `upload-flow.js` / `ocr.js` diagnostics 0 错误

## 已跳过
- 暂不在本轮继续拆分 OCR、同步、表单弹窗等高耦合链路
- 暂不在本轮引入数据库 schema 变更或同步协议变更

## 下一步
- 提交并推送本轮改动，确认线上部署版本升到 `2026-04-21 P3-6`
- 用户在线上强刷验证以下重点链路：
  - 识别前预览中逐条修改新增项后，保存结果是否按编辑后内容入库
  - 识别前预览中删除新增 / 合并 / 跳过项后，被删除项是否不会写入面板
  - 真实 PDF 会议安排表识别中，表格列、跨行参会人员和地点识别是否比 `P3-5` 更稳定
  - 微信上传页是否同样支持预览编辑后的保存结果
  - 页面底部附近的部署版本徽标是否显示 `2026-04-21 P3-6`
- 若线上复测稳定，再继续回头排查“跨日期会议点击完成后未打勾划线沉底”的剩余问题